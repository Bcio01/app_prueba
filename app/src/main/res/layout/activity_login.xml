<?xml version="1.0" encoding="utf-8"?>
<!-- Esta línea indica que es un archivo XML y define la codificación estándar UTF-8 -->

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_bg">
    <!--
        Este es el contenedor principal del diseño.
        Se llama ConstraintLayout y permite que los elementos se adapten
        automáticamente a distintos tamaños y orientaciones de pantalla.
        Esto significa que al girar el celular (horizontal o vertical),
        los objetos se mueven y ajustan sin desordenarse.
    -->

    <!-- Header con gradiente bibibi-->
    <View
        android:id="@+id/header"
        android:layout_width="0dp"
        android:layout_height="220dp"
        android:background="@drawable/bg_header"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
    <!--
        Este bloque crea el encabezado superior con fondo de color o gradiente.
        El ancho se pone en 0dp para que se ajuste al espacio entre los bordes izquierdo y derecho.
        Las líneas "Start_toStartOf" y "End_toEndOf" lo mantienen centrado horizontalmente.
        Así, el header se adapta a cualquier tamaño de pantalla.
    -->

    <!-- Título :) -->
    <TextView
        android:id="@+id/tvTitulo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/login_titulo"
        android:textColor="@color/on_primary"
        android:textSize="28sp"
        android:textStyle="bold"
        android:paddingStart="@dimen/space_20"
        android:paddingEnd="@dimen/space_20"
        app:layout_constraintBottom_toTopOf="@id/tvSubtitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/header"
        app:layout_constraintVertical_bias="0.75"/>
    <!--
        Este es el texto principal del título del login.
        Las restricciones hacen que siempre quede centrado en la parte superior:
        - "Start_toStartOf" y "End_toEndOf" lo centran horizontalmente.
        - "Top_toTopOf" lo fija dentro del header.
        - "Vertical_bias" controla su posición vertical de forma proporcional,
          permitiendo que se ajuste visualmente si la pantalla gira o cambia de tamaño.
        Esto asegura que el título no se mueva fuera de lugar ni se corte.
    -->

    <!-- Subtítulo -->
    <TextView
        android:id="@+id/tvSubtitulo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/login_subtitulo"
        android:textColor="@color/on_primary"
        android:alpha="0.9"
        android:paddingStart="@dimen/space_20"
        android:paddingEnd="@dimen/space_20"
        app:layout_constraintBottom_toBottomOf="@id/header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
    <!--
        Este es el texto del subtítulo.
        Está "anclado" al borde inferior del header, por lo que siempre se mantiene
        justo debajo del título aunque se cambie la orientación del dispositivo.
        También se centra automáticamente entre los bordes del layout.
    -->

    <!-- Tarjeta del formulario -->
    <LinearLayout
        android:id="@+id/tarjetaFormulario"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_card"
        android:orientation="vertical"
        android:elevation="6dp"
        android:translationY="-40dp"
        android:layout_marginStart="@dimen/space_16"
        android:layout_marginEnd="@dimen/space_16"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_max="@dimen/max_card_width">
        <!--
            Este bloque es la "tarjeta" blanca donde están los campos del login.
            - El ancho 0dp permite que se estire según las restricciones, adaptándose al espacio disponible.
            - Está anclada justo debajo del header ("Top_toBottomOf").
            - "Start_toStartOf" y "End_toEndOf" la centran horizontalmente.
            - "Width_max" limita su tamaño en pantallas muy anchas (como tablets o modo horizontal),
              para que no se vea desproporcionada.
            Gracias a estas líneas, el formulario se ajusta perfectamente en vertical y horizontal.
        -->

        <EditText
            android:id="@+id/edtEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/email_hint"
            android:inputType="textEmailAddress"
            android:textColor="@color/text_main"
            android:textColorHint="@color/text_dim"
            android:imeOptions="actionNext"
            android:paddingTop="@dimen/space_8"
            android:paddingBottom="@dimen/space_8"/>
        <!-- Campo de texto para ingresar el correo electrónico -->

        <EditText
            android:id="@+id/edtPass"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_12"
            android:hint="@string/password_hint"
            android:inputType="textPassword"
            android:textColor="@color/text_main"
            android:textColorHint="@color/text_dim"
            android:imeOptions="actionDone"
            android:paddingTop="@dimen/space_8"
            android:paddingBottom="@dimen/space_8"/>
        <!-- Campo de texto para ingresar la contraseña -->

        <Button
            android:id="@+id/btnLogin"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_20"
            android:background="@drawable/bg_button_primary"
            android:text="@string/btn_login"
            android:textColor="@color/on_primary"
            android:textStyle="bold"
            android:paddingTop="14dp"
            android:paddingBottom="14dp"/>
        <!-- Botón principal para iniciar sesión -->

        <TextView
            android:id="@+id/tvRecuperarpass"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_12"
            android:text="@string/olviste_pass"
            android:textColor="@color/brand_primary"/>
        <!-- Texto para recuperar la contraseña -->

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_12"
            android:gravity="center"
            android:orientation="horizontal">
            <!-- Contenedor horizontal para los textos de “no tienes cuenta” y “crear cuenta” -->

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/no_cuenta"
                android:textColor="@color/text_dim"/>
            <!-- Texto informativo -->

            <TextView
                android:id="@+id/tvCrear"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/crear_cuenta"
                android:textStyle="bold"
                android:paddingStart="@dimen/space_8"
                android:textColor="@color/brand_primary"/>
            <!-- Enlace de texto para crear una nueva cuenta -->
        </LinearLayout>
    </LinearLayout>
    <!-- Fin de la tarjeta del formulario -->

</androidx.constraintlayout.widget.ConstraintLayout>
    <!--
        En resumen:
        Este diseño usa ConstraintLayout para que todo el contenido se ajuste automáticamente.
        Las restricciones (Start, End, Top, Bottom) aseguran que cada elemento
        conserve su posición relativa al girar el celular.
        Además, el uso de 0dp y "width_max" evita deformaciones en pantallas grandes o apaisadas.
    -->

