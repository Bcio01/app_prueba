<?xml version="1.0" encoding="utf-8"?>
<!--
    Este archivo define el diseño principal de la pantalla HomeActivity.
    Usa un CoordinatorLayout como contenedor raíz y Material Design para la barra superior.
    Los botones están organizados en una cuadrícula (GridLayout) con estilo moderno.
-->

<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_bg"
    tools:context=".HomeActivity">

    <!-- ===================================================== -->
    <!--                BARRA SUPERIOR (TOOLBAR)               -->
    <!-- ===================================================== -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="0dp">

        <!--
            AppBarLayout contiene la barra superior.
            Sirve para colocar una Toolbar o un título.
            La propiedad "elevation" define la sombra bajo la barra (0dp = sin sombra).
        -->

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/brand_primary"
            android:title="Inicio"
            android:titleTextColor="@color/on_primary"
            app:titleCentered="true"/>
        <!--
            MaterialToolbar es la barra superior moderna de Material Design.
            Muestra el título centrado "Inicio" con color blanco y fondo personalizado.
        -->
    </com.google.android.material.appbar.AppBarLayout>

    <!-- ===================================================== -->
    <!--             CONTENIDO PRINCIPAL SCROLLABLE            -->
    <!-- ===================================================== -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:padding="24dp">

        <!--
            NestedScrollView permite desplazamiento del contenido (scroll).
            Ideal cuando hay muchos botones o textos.
            El padding (24dp) da espacio alrededor de todo el contenido.
        -->

        <LinearLayout
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal">

            <!--
                LinearLayout organiza los elementos en una sola columna vertical.
                Los elementos dentro estarán centrados horizontalmente.
            -->

            <!-- ===================================================== -->
            <!--                  TEXTO DE BIENVENIDA                 -->
            <!-- ===================================================== -->
            <TextView
                android:id="@+id/tvBienvenida"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Bienvenido 👋"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@color/text_main"
                android:layout_marginBottom="24dp"/>
            <!--
                Muestra el saludo principal al usuario.
                El tamaño 22sp lo hace más grande y legible.
                El margen inferior separa el texto de los botones.
            -->

            <!-- ===================================================== -->
            <!--                   SECCIÓN DE BOTONES                 -->
            <!-- ===================================================== -->
            <GridLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:columnCount="2"
                android:alignmentMode="alignMargins"
                android:useDefaultMargins="true"
                android:columnOrderPreserved="false">

                <!--
                    GridLayout permite organizar botones en varias columnas.
                    columnCount="2" crea dos columnas.
                    useDefaultMargins agrega espacio automático entre botones.
                -->

                <!-- Botón: Ir al perfil -->
                <Button
                    android:id="@+id/btnIrPerfil"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Perfil"/>
                <!--
                    Abre la pantalla PerfilActivity.
                    Usa el estilo MaterialComponents con fondo del color principal.
                -->

                <!-- Botón: Abrir sitio web -->
                <Button
                    android:id="@+id/btnAbrirWeb"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Sitio web"/>
                <!--
                    Abre una página web (Minecraft o cualquier URL definida).
                -->

                <!-- Botón: Enviar correo o SMS -->
                <Button
                    android:id="@+id/btnEnviarCorreo"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Correo"/>
                <!--
                    Abre la app de mensajes para enviar un SMS.
                -->

                <!-- Botón: Compartir texto -->
                <Button
                    android:id="@+id/btnCompartir"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Compartir"/>
                <!--
                    Comparte texto usando otras apps como WhatsApp o Gmail.
                -->

                <!-- Botón: Linterna -->
                <Button
                    android:id="@+id/btnLinterna"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Linterna"/>
                <!--
                    Enciende o apaga la linterna del teléfono.
                    Usa permisos de cámara para funcionar correctamente.
                -->

                <!-- Botón: Abrir cámara -->
                <Button
                    android:id="@+id/btnCamara"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Cámara"/>
                <!--
                    Abre la pantalla de cámara (CamaraActivity) para tomar fotos o grabar.
                -->

                <!-- Botón: Marcar teléfono -->
                <Button
                    android:id="@+id/btnMarcarTelefono"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Teléfono"/>
                <!--
                    Abre el marcador del teléfono con un número ya preparado.
                -->

                <!-- Botón: Seleccionar imagen de galería -->
                <Button
                    android:id="@+id/btnSeleccionarImagen"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Galería"/>
                <!--
                    Abre la galería del dispositivo para seleccionar una imagen.
                -->

                <!-- Botón: Abrir ubicación en Google Maps -->
                <Button
                    android:id="@+id/btnAbrirMaps"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Maps"/>
                <!--
                    Abre Google Maps mostrando una ubicación específica.
                    Si no hay app de mapas, abre el navegador con la URL.
                -->

                <!-- Botón: Configuración -->
                <Button
                    android:id="@+id/btnAbrirConfiguracion"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Configuración"/>
                <!--
                    Abre la pantalla ConfiguracionActivity.
                    Permite cambiar temas o preferencias del usuario.
                -->

                <!-- Botón: Ver detalles -->
                <Button
                    android:id="@+id/btnVerDetalles"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Detalles"/>
                <!--
                    Abre DetallesActivity y envía datos de ejemplo (id, título, etc.).
                -->

                <!-- Botón: Ayuda -->
                <Button
                    android:id="@+id/btnAbrirAyuda"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:backgroundTint="@color/brand_primary"
                    android:textColor="@color/on_primary"
                    android:text="Ayuda"/>
                <!--
                    Abre AyudaActivity, que explica cómo usar la aplicación.
                    Puede contener preguntas frecuentes o tutoriales.
                -->
            </GridLayout>
            <!-- Fin de la cuadrícula de botones -->
        </LinearLayout>
        <!-- Fin del contenedor vertical -->
    </androidx.core.widget.NestedScrollView>
    <!-- Fin del contenido desplazable -->

</androidx.coordinatorlayout.widget.CoordinatorLayout>
    <!-- Fin del diseño principal de la pantalla Home -->
